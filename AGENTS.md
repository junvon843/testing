# UV 学习项目规范

## 目录结构（不许乱放）

```
uv_learn/
├── AGENTS.md              ← 本文件：项目规范
├── src/                   ← 正式学习源码
│   └── （后续按模块放）
├── tests/                 ← 所有测试代码
│   └── （分门别类放）
├── scripts/               ← 临时脚本、单文件实验
│   └── （随便试，不许超出此目录）
├── notes/                 ← 学习笔记、文档
│   └── （文字类放这里）
├── play/                  ← 瞎折腾的实验区
│   └── （随便玩，玩坏了删掉重来）
├── pyproject.toml         ← uv 项目配置（由 uv init 生成，手动改需说明）
├── uv.lock                ← 锁定文件（由 uv 自动生成）
├── .python-version        ← Python 版本（由 uv python pin 生成）
├── README.md              ← 项目说明
└── .venv/                 ← 虚拟环境（由 uv 自动生成）
```

## 铁律

1. **测试代码只准放在 `tests/` 里**，不许在项目根目录乱丢 `test_xxx.py`。
2. **临时实验只准在 `play/` 或 `scripts/` 里**，其他目录保持整洁。
3. **修改 `pyproject.toml` 前必须说明原因**。
4. **运行命令前必须说明会生成什么、放在哪里**。
5. **不自动下载安装任何东西**，除非用户明确要求。
